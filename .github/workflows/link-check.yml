name: link-check

on:
  pull_request:
  push:
    branches: [main]
  schedule:
    - cron: "17 4 * * 1"

permissions:
  contents: read

jobs:
  links:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.12"
      - name: Test link-check policy
        run: python tools/test_check_links.py
      - name: Resolve every link, fail on rename redirects and archived targets
        env:
          # Lifts the unauthenticated GitHub API rate limit for the
          # archived-repository lookups in tools/check_links.py.
          GITHUB_TOKEN: ${{ github.token }}
        run: python tools/check_links.py
